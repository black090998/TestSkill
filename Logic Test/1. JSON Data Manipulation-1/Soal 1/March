const data = [
];

//  Maret
const purchasesInMarch = data.filter((order) => {
  const month = new Date(order.created_at).getMonth() + 1; // 1-12
  return month === 3; // 3 atau maret
});

console.log(purchasesInMarch);
